#################
# Bash Profile

# leave this? do this cci boxes only?
#[ "$TERM" = "xterm-color" ] && export TERM=xterm

. ${HOME}/.bashrc


### Added by the Heroku Toolbelt
export PATH="/usr/local/heroku/bin:$PATH"
